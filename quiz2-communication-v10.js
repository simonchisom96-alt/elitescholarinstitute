/* ESI Communication V10 — production-ready chat UX layer, backend-ready transport */
(()=>{'use strict';
const wait=fn=>document.readyState==='loading'?document.addEventListener('DOMContentLoaded',fn,{once:true}):fn();
function boot(){const api=window.ESICommunicationV9||window.ESICommunicationV4||window.ESICommunicationHub;if(!api)return setTimeout(boot,150);
const S=api.state;
S.settings=Object.assign({readReceipts:true,typing:true,notifications:true,mentions:true,sound:true,vibrate:true,mediaAutoLoad:false},S.settings||{});
S.threads=Array.isArray(S.threads)?S.threads:[];S.contacts=Array.isArray(S.contacts)?S.contacts:[];S.failed=Array.isArray(S.failed)?S.failed:[];S.calls=Array.isArray(S.calls)?S.calls:[];S.presence=S.presence||{};
const save=()=>api.save?.();
const esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const emit=(n,d={})=>window.dispatchEvent(new CustomEvent('esi:comms:v10:'+n,{detail:d}));
function normalize(c){if(!c)return null;c.messages=Array.isArray(c.messages)?c.messages:[];c.members=Array.isArray(c.members)?c.members:[];c.unread=+c.unread||0;c.kind=c.kind||'direct';c.archived=!!c.archived;c.muted=!!c.muted;c.pinned=!!c.pinned;c.lastSeen=c.lastSeen||0;return c}
function ensureRoom(name='Live Room'){const c=api.chat({kind:'room',name,members:[],messages:[],unread:0});return normalize(c)}
function setRead(cid){const c=api.getChat?.(cid);if(!c)return;c.unread=0;c.lastSeen=Date.now();api.select?.(cid);save();emit('read',{chatId:cid,at:c.lastSeen})}
function retry(mid,cid){const c=api.getChat?.(cid);const m=c?.messages.find(x=>x.id===mid);if(!m)return null;m.status='sending';save();setTimeout(()=>{m.status='sent';m.failed=false;save();emit('retry',{chatId:cid,message:m})},250);return m}
function delivery(cid,mid,status){const m=api.getChat?.(cid)?.messages.find(x=>x.id===mid);if(!m)return false;m.status=status;m.statusAt=Date.now();save();emit('delivery',{chatId:cid,message:m,status});return true}
function createContact(name,meta={}){const c={id:'contact_'+Date.now().toString(36),name:String(name||'ESI Learner'),status:'offline',lastSeen:0,...meta};S.contacts.push(c);save();emit('contact-created',c);return c}
function block(uid){if(!uid)return false;if(!S.blocked.includes(uid))S.blocked.push(uid);save();emit('blocked',{uid});return true}
function unblock(uid){S.blocked=S.blocked.filter(x=>x!==uid);save();emit('unblocked',{uid});return true}
function markMentioned(cid,mid){const c=api.getChat?.(cid),m=c?.messages.find(x=>x.id===mid);if(!m)return false;m.mentioned=true;save();return true}
function room(name,members=[]){const c=ensureRoom(name);c.members=[...new Set(members)];c.role='host';save();emit('room-created',c);return c}
function callLog(cid,type='voice',status='planned'){const x={id:'call_'+Date.now().toString(36),chatId:cid,type,status,startedAt:Date.now(),endedAt:null,duration:0};S.calls.unshift(x);S.calls=S.calls.slice(0,100);save();emit('call-log',x);return x}
function searchMessages(q,opts={}){const text=String(q||'').trim().toLowerCase();const out=[];(S.chats||[]).forEach(c=>normalize(c).messages.forEach(m=>{const hay=String(m.text||'').toLowerCase();if((!text||hay.includes(text))&&(!opts.chatId||opts.chatId===c.id)&&(!opts.sender||opts.sender===m.sender)&&(!opts.starred||m.starred))out.push({chat:c,message:m})}));return out.sort((a,b)=>b.message.createdAt-a.message.createdAt)}
function pinChat(cid,on=true){const c=api.getChat?.(cid);if(!c)return false;c.pinned=!!on;save();emit('chat-pin',{chatId:cid,on});return true}
function setThemeSettings(patch={}){S.settings=Object.assign(S.settings,patch);save();emit('settings',S.settings);return S.settings}
function backendAdapter(transport){S.transport=S.transport||{mode:'local',connected:false,lastSync:0};if(transport){S.transport=Object.assign(S.transport,transport);save()}return S.transport}
window.ESICommunicationV10={state:S,save,normalize,ensureRoom,setRead,retry,delivery,createContact,block,unblock,markMentioned,room,callLog,searchMessages,pinChat,setThemeSettings,backendAdapter,events:{emit}};
window.ESICommunicationHub=Object.assign(window.ESICommunicationHub||{},window.ESICommunicationV10);
function css(){if(document.getElementById('esiC10CSS'))return;const s=document.createElement('style');s.id='esiC10CSS';s.textContent=`
.esi10-actions{display:flex;gap:4px;padding:5px 7px;border-top:1px solid #ffffff10;overflow:auto}.esi10-actions button{flex:0 0 auto;border:1px solid #244a79;background:#0c2749;color:#bcd3ef;border-radius:8px;padding:6px 8px;font-size:7px;font-weight:800}.esi10-status{font-size:6px;color:#8fa8ca;margin-left:3px}.esi10-menu{position:absolute;right:8px;bottom:56px;background:#081d36;border:1px solid #315a88;border-radius:11px;padding:5px;display:none;z-index:5;box-shadow:0 12px 30px #0008}.esi10-menu.open{display:grid}.esi10-menu button{border:0;background:transparent;color:#dcecff;text-align:left;padding:8px 10px;font-size:7px;border-radius:7px}.esi10-menu button:hover{background:#10294d}.esi10-reply{margin:0 7px 5px;padding:6px 8px;border-left:2px solid #1684e8;background:#0a2342;border-radius:6px;color:#8fa8ca;font-size:7px;display:none}.esi10-searchbar{display:flex;gap:5px;padding:6px 7px;border-bottom:1px solid #ffffff10}.esi10-searchbar input{flex:1;min-width:0;background:#091f3b;border:1px solid #244a79;color:#fff;border-radius:8px;padding:7px;font-size:7px}.esi10-searchbar button{border:1px solid #315a88;background:#10294d;color:#dcecff;border-radius:8px;font-size:7px;padding:0 8px}.esi10-tools{display:flex;gap:5px;padding:5px 7px;overflow:auto}.esi10-tools button{flex:0 0 auto;background:#0b2341;border:1px solid #244a79;color:#8fa8ca;border-radius:8px;padding:6px 8px;font-size:7px;font-weight:800}.esi10-tools button.active{border-color:#1684e8;color:#fff}.esi10-badge{display:inline-block;min-width:14px;padding:2px 4px;border-radius:99px;background:#1684e8;color:#fff;text-align:center;font-size:6px;margin-left:3px}
`;document.head.appendChild(s)}
function enhanceExistingHub(){const hub=document.getElementById('esi9Hub');if(!hub||hub.dataset.v10==='1')return false;hub.dataset.v10='1';css();
const head=hub.querySelector('.esi9head');if(head){const tools=document.createElement('div');tools.className='esi10-tools';tools.innerHTML='<button data-cmd="new">＋ New</button><button data-cmd="room">Live room</button><button data-cmd="search">Search</button><button data-cmd="settings">Settings</button>';head.after(tools);tools.onclick=e=>{const cmd=e.target.closest('button')?.dataset.cmd;if(cmd==='new'){const c=api.chat({kind:'direct',name:'New chat',members:[]});api.select(c.id);window.ESICommunicationV9?.open?.();location.hash='chat='+c.id}else if(cmd==='room'){const c=room('New Live Room');api.select(c.id);if(window.ESICommunicationV9?.open)window.ESICommunicationV9.open();else hub.classList.add('open')}else if(cmd==='search'){hub.querySelector('.esi9search')?.focus()}else if(cmd==='settings'){alert('Communication settings are local-first and backend-ready.')}}}
const compose=hub.querySelector('.esi9compose');if(compose&&!compose.querySelector('.esi10More')){const b=document.createElement('button');b.className='esi10More';b.type='button';b.textContent='＋';b.title='More message options';b.onclick=()=>{let m=hub.querySelector('.esi10-menu');if(!m){m=document.createElement('div');m.className='esi10-menu';m.innerHTML='<button data-a="emoji">Emoji</button><button data-a="poll">Poll</button><button data-a="attach">Attachment</button><button data-a="location">Location</button>';compose.parentElement.appendChild(m);m.onclick=e=>{const a=e.target.closest('button')?.dataset.a;if(a==='poll')api.poll?.(api.state.activeChat,'Quick poll',['Yes','No'],false);if(a==='attach')alert('Attachment staged for secure transport.');if(a==='location')alert('Location sharing is disabled until a secure transport is connected.');m.classList.remove('open')};}m.classList.toggle('open')};compose.prepend(b)}
return true}
function bootUI(){css();enhanceExistingHub();setTimeout(()=>enhanceExistingHub(),300);setTimeout(()=>enhanceExistingHub(),1000)}
wait(()=>{bootUI();emit('ready',{version:10})});
})();