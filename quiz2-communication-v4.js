/* ESI Quiz Studio Communication V9 — unified communication + workspace presentation */
(()=>{'use strict';
const KEY='esi.communication.v9',OLD=['esi.communication.v4','esi.quiz2.chat.v2','esi.quiz2.chat.v3'];
const clone=x=>{try{return structuredClone(x)}catch{return JSON.parse(JSON.stringify(x))}},read=k=>{try{return JSON.parse(localStorage.getItem(k)||'null')}catch{return null}},id=p=>p+'_'+Date.now().toString(36)+'_'+Math.random().toString(36).slice(2,8)};
const defaults={version:9,activeChat:null,chats:[],groups:[],rooms:[],notifications:[],drafts:{},typing:{},presence:{},starred:[],pinned:[],archived:[],muted:[],pending:[],polls:[],blocked:[],reported:[],settings:{readReceipts:true,typing:true,notifications:true,mentions:true,sound:true,vibrate:true,mediaAutoLoad:false},profile:{name:'ESI Learner',status:'Available'}};
let state=Object.assign(clone(defaults),read(KEY)||{});
function save(){try{localStorage.setItem(KEY,JSON.stringify(state))}catch{}}
function emit(n,d={}){try{window.dispatchEvent(new CustomEvent('esi:comms:'+n,{detail:d}))}catch{}}
function migrate(){OLD.forEach(k=>{const o=read(k);if(!o)return;(o.chats||[]).forEach(c=>{if(c&&!state.chats.some(x=>x.id===c.id))state.chats.push(clone(c))});(o.notifications||[]).forEach(n=>state.notifications.push(clone(n)))});state.chats.forEach(c=>{c.messages=Array.isArray(c.messages)?c.messages:[];c.members=Array.isArray(c.members)?c.members:[];c.unread=+c.unread||0;c.kind=c.kind||'direct'});state.notifications=state.notifications.slice(0,300);save()}
function chat(data={}){let c=data.id&&state.chats.find(x=>x.id===data.id);if(!c){c=Object.assign({id:id('chat'),kind:'direct',name:'New conversation',avatar:'ESI',members:[],messages:[],unread:0,muted:false,pinned:false,archived:false,createdAt:Date.now()},data);state.chats.push(c)}else Object.assign(c,data);save();emit('chat-updated',c);return c}
const get=idv=>state.chats.find(c=>c.id===idv);
function message(data={}){return Object.assign({id:id('msg'),text:'',sender:'me',createdAt:Date.now(),status:'sent',editedAt:null,deletedAt:null,replyTo:null,forwarded:false,forwardedFrom:null,reactions:{},starred:false,pinned:false,mentions:[],attachment:null},data)}
function send(cid,text,meta={}){const c=get(cid||state.activeChat);if(!c)return null;const t=String(text||'').trim();if(!t&&!meta.attachment)return null;const m=message(Object.assign({text:t},meta));c.messages.push(m);state.activeChat=c.id;state.drafts[c.id]='';save();emit('message-sent',{chat:c,message:m});return m}
function receive(cid,text,meta={}){const c=get(cid);if(!c)return null;const m=message(Object.assign({text:String(text||''),sender:meta.sender||'member',status:'received'},meta));c.messages.push(m);if(state.activeChat!==cid&&!c.muted)c.unread++;save();emit('message-received',{chat:c,message:m});return m}
function edit(cid,mid,text){const m=get(cid)?.messages.find(x=>x.id===mid);if(!m||m.deletedAt)return null;m.text=String(text||'').trim();m.editedAt=Date.now();save();emit('message-edited',{chatId:cid,message:m});return m}
function remove(cid,mid,all=false){const c=get(cid),m=c?.messages.find(x=>x.id===mid);if(!m)return false;if(all){m.deletedAt=Date.now();m.text='';m.attachment=null}else c.messages=c.messages.filter(x=>x.id!==mid);save();emit('message-removed',{chatId:cid,messageId:mid,forEveryone:all});return true}
function react(cid,mid,emoji){const m=get(cid)?.messages.find(x=>x.id===mid);if(!m)return null;m.reactions=m.reactions||{};m.reactions[emoji]=(m.reactions[emoji]||0)+1;save();emit('reaction',{chatId:cid,messageId:mid,emoji});return m.reactions}
function flag(cid,mid,type){const m=get(cid)?.messages.find(x=>x.id===mid);if(!m)return false;if(type==='star'){m.starred=!m.starred;m.starred?state.starred.push(mid):state.starred=state.starred.filter(x=>x!==mid)}if(type==='pin'){m.pinned=!m.pinned;m.pinned?state.pinned.push(mid):state.pinned=state.pinned.filter(x=>x!==mid)}save();emit(type,{chatId:cid,message:m});return true}
function reply(cid,mid){const m=get(cid)?.messages.find(x=>x.id===mid);if(!m)return null;state.replyTo={chatId:cid,messageId:mid};save();emit('reply-target',state.replyTo);return m}
function forward(sc,mid,tc){const m=get(sc)?.messages.find(x=>x.id===mid),t=get(tc);return m&&t?send(tc,m.text,{forwarded:true,forwardedFrom:get(sc).name,attachment:m.attachment}):null}
function select(cid){const c=get(cid);if(!c)return null;state.activeChat=cid;c.unread=0;save();emit('chat-selected',c);emit('read',{chatId:cid,at:Date.now()});return c}
function draft(cid,text){state.drafts[cid]=String(text||'');save();emit('draft',{chatId:cid,text:state.drafts[cid]})}
function typing(cid,names,on=true){if(on)state.typing[cid]=(Array.isArray(names)?names:[names||'Someone']).filter(Boolean).slice(0,5);else delete state.typing[cid];save();emit('typing',{chatId:cid,names:state.typing[cid]||[],on})}
function presence(uid,status){state.presence[uid]={status,at:Date.now()};save();emit('presence',{uid,status})}
function mute(cid,on=true){const c=get(cid);if(!c)return false;c.muted=on;on&&!state.muted.includes(cid)&&state.muted.push(cid);!on&&(state.muted=state.muted.filter(x=>x!==cid));save();emit('mute',{chatId:cid,on});return on}
function archive(cid,on=true){const c=get(cid);if(!c)return false;c.archived=on;on&&!state.archived.includes(cid)&&state.archived.push(cid);!on&&(state.archived=state.archived.filter(x=>x!==cid));save();emit('archive',{chatId:cid,on});return on}
function notify(title,body,type='message',meta={}){const n={id:id('note'),title:String(title||''),body:String(body||''),type,meta,read:false,createdAt:Date.now()};state.notifications.unshift(n);state.notifications=state.notifications.slice(0,300);save();emit('notification',n);return n}
function markAll(){state.notifications.forEach(n=>n.read=true);save();emit('notifications-read')}
function search(q){const t=String(q||'').toLowerCase(),out=[];state.chats.forEach(c=>c.messages.forEach(m=>{if(!t||String(m.text||'').toLowerCase().includes(t))out.push({chat:c,message:m})}));return out}
function group(name,members=[],meta={}){const c=chat({kind:'group',name:String(name||'Study Group'),members:[...members],description:meta.description||'',admins:meta.admins||[],role:'admin'});state.groups.push(c.id);save();emit('group-created',c);return c}
function poll(cid,question,options=[],multi=false){const p={id:id('poll'),chatId:cid,question:String(question||''),options:options.slice(0,10).map(x=>({id:id('opt'),label:String(x),votes:0})),multi:!!multi,voters:{},closed:false,createdAt:Date.now()};state.polls.unshift(p);save();emit('poll-created',p);return p}
function queue(kind,payload){const j={id:id('job'),kind,payload,status:'queued',attempts:0,createdAt:Date.now()};state.pending.push(j);save();emit('queue',j);return j}
function unread(){return state.chats.reduce((n,c)=>n+(+c.unread||0),0)+state.notifications.filter(n=>!n.read).length}
function api(){return{state,save,chat,getChat:get,send,receive,edit,remove,react,flag,reply,forward,select,draft,typing,presence,mute,archive,notify,markAll,search,group,poll,queue,unread}}
function boot(){migrate();window.ESICommunicationV9=api();window.ESICommunicationV4=api();window.ESICommunicationHub=Object.assign(window.ESICommunicationHub||{},api());emit('ready',{version:9,state})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();

/* V9 presentation: communication hub, command center and clean builder entry points. */
function css(){if(document.getElementById('esiC9CSS'))return;const s=document.createElement('style');s.id='esiC9CSS';s.textContent=`.esi9-fab{position:fixed;right:12px;bottom:76px;z-index:700;width:50px;height:50px;border-radius:16px;background:linear-gradient(135deg,#ffd84d,#f6c800);color:#07182d;font-size:20px;box-shadow:0 12px 35px #0008;border:1px solid #fff6}.esi9{position:fixed;inset:64px 8px 70px auto;width:min(470px,calc(100vw - 16px));z-index:690;background:#071a32;border:1px solid #2d6196;border-radius:20px;box-shadow:0 25px 80px #000a;display:none;overflow:hidden}.esi9.open{display:flex;flex-direction:column}.esi9head{display:flex;gap:7px;align-items:center;padding:10px 12px;border-bottom:1px solid #ffffff14}.esi9head b{flex:1;font-size:10px;color:#fff}.esi9tabs{display:flex;gap:4px;padding:7px;overflow:auto;border-bottom:1px solid #ffffff12}.esi9tabs button{flex:0 0 auto;padding:8px 10px;border-radius:9px;border:1px solid #244a79;background:#0a2342;color:#8fa8ca;font-size:7px;font-weight:900}.esi9tabs button.on{background:#1264b733;border-color:#1684e8;color:#fff}.esi9body{display:flex;flex:1;min-height:0}.esi9list{width:145px;overflow:auto;padding:6px;border-right:1px solid #ffffff12}.esi9item{width:100%;display:block;text-align:left;padding:9px;border:0;border-radius:10px;background:transparent;color:#b8cde7;font-size:7px;margin:2px 0}.esi9item.on{background:#0e3158;color:#fff}.esi9item b{display:block;font-size:8px}.esi9item span{display:block;color:#7897bc;margin-top:2px}.esi9thread{display:flex;flex-direction:column;flex:1;min-width:0}.esi9search{margin:7px;background:#091f3b;border:1px solid #244a79;border-radius:9px;padding:8px;color:#fff;font-size:8px}.esi9msgs{flex:1;overflow:auto;padding:8px}.esi9msg{max-width:84%;padding:8px 9px;border-radius:12px;background:#10294d;margin:5px 0;font-size:8px;line-height:1.4}.esi9msg.me{margin-left:auto;background:#0b4ea2}.esi9msg small{display:block;color:#8fa8ca;font-size:6px;margin-top:3px}.esi9compose{display:flex;gap:5px;padding:7px;border-top:1px solid #ffffff14}.esi9compose input{flex:1;min-width:0;background:#091f3b;border:1px solid #244a79;border-radius:9px;color:#fff;padding:9px;font-size:8px}.esi9compose button,.esi9new{background:#1684e8;color:#fff;border:0;border-radius:9px;padding:0 11px;font-size:8px;font-weight:900}.esi9empty{padding:18px;color:#8fa8ca;font-size:8px;text-align:center}.esi9card{margin:8px;padding:10px;border:1px solid #244a79;border-radius:12px;background:#09203c}.esi9card b{font-size:9px;color:#f6c800}.esi9card p{font-size:7px;color:#8fa8ca;line-height:1.5}.esi9actions{display:grid;grid-template-columns:1fr 1fr;gap:6px;margin-top:8px}.esi9actions button{padding:9px;border-radius:9px;background:#0c2749;border:1px solid #315a88;color:#dcecff;font-size:7px;font-weight:800}.esi9compose button{min-width:54px}.esi9dot{display:inline-block;width:6px;height:6px;border-radius:50%;background:#20d889;margin-right:4px}`;document.head.appendChild(s)}
function renderHub(tab='chats'){css();let p=document.getElementById('esi9Hub');if(!p){const f=document.createElement('button');f.id='esi9Fab';f.className='esi9-fab';f.textContent='💬';f.title='Communication';f.setAttribute('aria-label','Open ESI Communication');document.body.appendChild(f);p=document.createElement('section');p.id='esi9Hub';p.className='esi9';p.innerHTML='<header class="esi9head"><b>ESI Communication</b><span class="esi9dot"></span><button class="close" id="esi9Close">×</button></header><nav class="esi9tabs"></nav><div class="esi9body"><aside class="esi9list"></aside><main class="esi9thread"><input class="esi9search" placeholder="Search messages…"><div class="esi9msgs"></div><div class="esi9compose"><input placeholder="Write a message…"><button>Send</button></div></main></div>';document.body.appendChild(p);f.onclick=()=>{p.classList.toggle('open');renderHub()};p.querySelector('#esi9Close').onclick=()=>p.classList.remove('open');p.querySelector('.esi9search').oninput=()=>renderHub(tab);p.querySelector('.esi9compose input').onkeydown=e=>{if(e.key==='Enter')doSend()};p.querySelector('.esi9compose button').onclick=doSend}
const tabs=['chats','groups','rooms','notifications','saved','archived'];p.querySelector('.esi9tabs').innerHTML=tabs.map(t=>`<button class="${tab===t?'on':''}" data-t="${t}">${t[0].toUpperCase()+t.slice(1)}${t==='notifications'&&state.notifications.some(n=>!n.read)?' •':''}</button>`).join('');p.querySelectorAll('[data-t]').forEach(b=>b.onclick=()=>renderHub(b.dataset.t));
const list=p.querySelector('.esi9list'),thread=p.querySelector('.esi9thread');let items=[];
if(tab==='notifications'){items=state.notifications.map(n=>({id:n.id,name:n.title,sub:n.body,notification:n}))}else if(tab==='saved'){state.chats.forEach(c=>c.messages.filter(m=>m.starred).forEach(m=>items.push({id:c.id,name:c.name,sub:m.text,chat:c,message:m})))}else if(tab==='archived'){items=state.chats.filter(c=>c.archived).map(c=>({id:c.id,name:c.name,sub:'Archived',chat:c}))}else{items=state.chats.filter(c=>tab==='chats'?c.kind==='direct':tab==='groups'?c.kind==='group':c.kind==='room').map(c=>({id:c.id,name:c.name,sub:(c.messages.at(-1)?.text||'No messages')+(c.unread?' • '+c.unread+' unread':''),chat:c}))}
if(!items.length){list.innerHTML='<div class="esi9empty">Nothing here yet.</div>'}else list.innerHTML=items.map(x=>`<button class="esi9item ${x.chat?.id===state.activeChat?'on':''}" data-i="${x.id}"><b>${esc9(x.name)}</b><span>${esc9(x.sub||'')}</span></button>`).join('');list.querySelectorAll('[data-i]').forEach(b=>b.onclick=()=>{const x=items.find(i=>i.id===b.dataset.i);if(x?.chat){select(x.chat.id);renderHub(tab)}else if(x?.notification){x.notification.read=true;save();renderHub(tab)}});
const c=get(state.activeChat)||items.find(x=>x.chat)?.chat;if(tab==='notifications'||tab==='saved'||tab==='archived'&&!c){thread.querySelector('.esi9msgs').innerHTML='<div class="esi9card"><b>'+tab[0].toUpperCase()+tab.slice(1)+'</b><p>Select an item from the left to view it. Your communication workspace keeps conversations, alerts and saved items separated so the main builder stays uncluttered.</p></div>'}else{thread.querySelector('.esi9msgs').innerHTML=(c?.messages||[]).map(m=>`<div class="esi9msg ${m.sender==='me'?'me':''}">${m.deletedAt?'<i>Message removed</i>':esc9(m.text)}<small>${new Date(m.createdAt).toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'})}${m.editedAt?' · edited':''}${m.starred?' · ★':''}${m.pinned?' · 📌':''}</small></div>`).join('')||'<div class="esi9empty">Start a conversation.</div>';thread.querySelector('.esi9msgs').scrollTop=thread.querySelector('.esi9msgs').scrollHeight}}
function esc9(x){return String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function doSend(){const p=document.getElementById('esi9Hub'),i=p?.querySelector('.esi9compose input'),t=i?.value.trim();if(!t)return;if(!state.activeChat)chat({name:'ESI Support'});send(state.activeChat,t);i.value='';renderHub('chats')}
function builder(){if(document.getElementById('esi9BuilderNav'))return;const main=document.querySelector('.main');if(!main)return;const wrap=document.createElement('section');wrap.id='esi9BuilderNav';wrap.className='panel';wrap.innerHTML='<div class="title"><div><h2>Studio workspace</h2><p>One clear path from idea to published quiz.</p></div></div><div class="esi9actions"><button data-s="create">Create</button><button data-s="build">Build</button><button data-s="enhance">Enhance</button><button data-s="logic">Logic</button><button data-s="review">Review</button><button data-s="publish">Publish</button><button data-s="results">Results</button><button data-s="communicate">Communication</button></div>';const anchor=main.querySelector('.hero');anchor?.after(wrap);wrap.querySelector('[data-s="communicate"]').onclick=()=>{const f=document.getElementById('esi9Fab');f?.click()};wrap.querySelectorAll('[data-s]').forEach(b=>b.onclick=()=>{const key=b.dataset.s;const q=Array.from(document.querySelectorAll('.panel,.workspace,.nexus,.toolbox,.properties')).find(e=>new RegExp(key,'i').test(e.innerText||''));q?.scrollIntoView({behavior:'smooth',block:'start'})})}
function bootUI(){setTimeout(()=>{builder();css();renderHub();document.getElementById('esi9Hub')?.classList.remove('open')},300)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bootUI,{once:true});else bootUI();
})();
