# Elite Scholar Institute Android APK

This file documents the APK delivery path. The web site does not use a service worker.

The APK is built by the GitHub Actions Android workflow. The resulting APK must be published as a GitHub Release asset before the website's Install Now action can point to a stable public APK URL.
