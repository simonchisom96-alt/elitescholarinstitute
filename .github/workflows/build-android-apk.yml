name: Elite Scholar Institute Android APK

on:
  push:
    branches:
      - main
    paths:
      - 'android/**'
      - 'app.js'
      - 'sw.js'
      - 'downloader.js'
      - 'manifest.json'
      - 'logo.jpg'
      - 'Textbooks.html'
      - '.github/workflows/build-android-apk.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: android-apk-main
  cancel-in-progress: false

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Verify source versions and textbook route
        shell: bash
        run: |
          set -euo pipefail
          echo "app.js BUILD:"; grep -m1 "const BUILD" app.js || true
          echo "sw cache:"; grep -m1 "CACHE_VERSION" sw.js || true
          echo "downloader cache:"; grep -m1 "CACHE_NAME" downloader.js || true
          echo "Android version:"; grep -E "versionCode|versionName" android/app/build.gradle.kts
          echo "English textbook route:"; grep -m1 -o 'href="[^"]*Englishtextbook\.html"' Textbooks.html || true

      - name: Bundle only the defined ESI offline shell
        shell: bash
        run: |
          set -euo pipefail
          rm -rf android/app/src/main/assets
          mkdir -p android/app/src/main/assets/site
          cat > /tmp/esi-shell.txt <<'EOF'
          index.html
          logo.jpg
          advert.png
          esi.jpg
          founder.jpg
          manifest.json
          offline.html
          app.js
          downloader.js
          Textbooks.html
          uniben.html
          crstextbook.html
          literaturetextbook.html
          postutme.html
          buk.html
          imsu.html
          oou.html
          eksu.html
          esut.html
          lautech.html
          aaua.html
          syllables.html
          Grace.jpg
          Chuka.jpg
          325.jpg
          339.jpg
          faloke.jpg
          emma.jpg
          uniport.html
          unijos.html
          unimed.html
          unical.html
          yabatech.html
          oauth.html
          luth.html
          delsu.html
          governmenttextbook.html
          Englishtextbook.html
          biologytextbook.html
          chemistrytextbook.html
          economicstextbook.html
          physicstextbook.html
          mathematicstextbook.html
          motivation.html
          chapter2.html
          chapter3.html
          chapter4.html
          chapter5.html
          chapter6.html
          chapter7.html
          chapter8.html
          chapter9.html
          chapter10.html
          furthermathstextbook.html
          accountingtextbook.html
          motivation2.html
          motivation3.html
          biop.html
          motivatio.html
          uip.html
          ulp.html
          picture.html
          motivation4.html
          unn.html
          physicsp.html
          chemp.html
          crsp.html
          englishp.html
          literaturep.html
          mathematicsp.html
          governmentp.html
          economicsp.html
          accountingp.html
          oau.html
          notification.html
          abu.html
          futa.html
          unilorin.html
          unizik.html
          password.js
          credit.html
          timetable1.jpg
          timetable2.jpg
          quiz1.html
          quiz.html
          video.mp4
          firebase-config.js
          multiplayer.js
          singleplay.js
          EOF
          sed -i 's/^[[:space:]]*//' /tmp/esi-shell.txt
          while IFS= read -r file; do
            [ -z "$file" ] && continue
            test -f "$file" || { echo "Missing required shell file: $file"; exit 1; }
          done < /tmp/esi-shell.txt
          rsync -a --files-from=/tmp/esi-shell.txt ./ android/app/src/main/assets/site/
          cp -f logo.jpg android/app/src/main/res/drawable/logo.jpg
          echo "Bundled shell file count: $(find android/app/src/main/assets/site -type f | wc -l)"
          echo "Bundled shell size: $(du -sh android/app/src/main/assets/site | cut -f1)"

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'

      - name: Build ESI APK
        working-directory: android
        run: gradle :app:assembleDebug --no-daemon --stacktrace

      - name: Verify APK
        shell: bash
        run: |
          set -euo pipefail
          test -s android/app/build/outputs/apk/debug/app-debug.apk
          mkdir -p release
          cp android/app/build/outputs/apk/debug/app-debug.apk release/ESI.apk
          ls -lh release/ESI.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: ESI-APK-v3.8
          path: release/ESI.apk
          if-no-files-found: error
          retention-days: 30

      - name: Confirm successful APK build
        shell: bash
        run: echo "ESI v3.8 APK build and verification completed successfully."
